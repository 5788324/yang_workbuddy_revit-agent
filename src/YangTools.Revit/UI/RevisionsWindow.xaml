<Window x:Class="YangTools.Revit.UI.RevisionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择修订 (Select Revisions)" Height="400" Width="400"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">

    <Window.Resources>
        <!-- Changan Theme Resources -->
        <SolidColorBrush x:Key="WindowBg" Color="#FAF6F0"/>
        <SolidColorBrush x:Key="WindowBorder" Color="#D5C8B8"/>
        <SolidColorBrush x:Key="PanelBg" Color="#F0EAE0"/>
        <SolidColorBrush x:Key="PrimaryText" Color="#3A3028"/>
        <SolidColorBrush x:Key="SecondaryText" Color="#8A7A68"/>
        <SolidColorBrush x:Key="BtnBg" Color="#E8DFD0"/>
        <SolidColorBrush x:Key="BtnBorder" Color="#C8B8A0"/>
        <SolidColorBrush x:Key="BtnHover" Color="#DDD0C0"/>

        <Style TargetType="{x:Type Button}">
            <Setter Property="Background" Value="{StaticResource BtnBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BtnBorder}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource BtnHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource WindowBg}" BorderBrush="{StaticResource WindowBorder}" BorderThickness="1" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" CornerRadius="8,8,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#8B6D4C" Offset="0.0" />
                        <GradientStop Color="#C4A67A" Offset="1.0" />
                    </LinearGradientBrush>
                </Border.Background>
                <TextBlock Text="选择修订 (Select Revisions)" Foreground="White" VerticalAlignment="Center" Margin="10,0,0,0" FontWeight="SemiBold"/>
            </Border>

            <!-- Revisions List -->
            <ListBox x:Name="RevisionsList" Grid.Row="1" Margin="10" Background="{StaticResource PanelBg}" BorderBrush="{StaticResource WindowBorder}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding DisplayName}" IsChecked="{Binding IsSelected}" Margin="5" Foreground="{StaticResource PrimaryText}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10,0">
                <Button Content="确定 (OK)" Width="80" Click="OkButton_Click" IsDefault="True"/>
                <Button Content="取消 (Cancel)" Width="80" Click="CancelButton_Click" IsCancel="True"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
