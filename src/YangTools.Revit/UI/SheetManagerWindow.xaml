<Window x:Class="YangTools.Revit.UI.SheetManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="图纸与图框管理 (Sheet &amp; Titleblock Manager)" Height="700" Width="1100"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="SharedStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

        <Style TargetType="{x:Type DataGrid}" BasedOn="{StaticResource SharedDataGridStyle}">
        </Style>
        
        <Style TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource SharedDataGridColumnHeaderStyle}">
            <Setter Property="ContextMenu">
                <Setter.Value>
                    <ContextMenu>
                        <MenuItem Header="隐藏/删除此列 (Hide Column)">
                            <MenuItem.Style>
                                <Style TargetType="{x:Type MenuItem}">
                                    <EventSetter Event="Click" Handler="HideColumn_Click" />
                                </Style>
                            </MenuItem.Style>
                        </MenuItem>
                    </ContextMenu>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type TabControl}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style TargetType="{x:Type TabItem}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{StaticResource SecondaryText}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Border Name="Border" Background="{TemplateBinding Background}" BorderBrush="{StaticResource WindowBorder}" BorderThickness="0,0,0,2">
                            <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F8F3EC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="{StaticResource WindowBg}" BorderBrush="{StaticResource WindowBorder}" BorderThickness="1" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" CornerRadius="8,8,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"
                    Background="{DynamicResource TitleBarGradient}">
                <Grid>
                    <TextBlock Text="图纸与图框管理 (Sheet &amp; Titleblock Manager)" Foreground="White" VerticalAlignment="Center" Margin="15,0,0,0" FontWeight="SemiBold" FontSize="14"/>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" Width="30" Height="30" Padding="0"
                            Background="Transparent" BorderThickness="0" Click="CloseButton_Click">
                        <TextBlock Text="&#xE711;" FontFamily="Segoe MDL2 Assets" Foreground="White" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <Button.Style>
                            <Style TargetType="{x:Type Button}">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type Button}">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="15">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource DangerBg}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <TabControl Grid.Row="1" Margin="10,0,10,10" SelectionChanged="TabControl_SelectionChanged">
                <!-- 图纸管理 Tab -->
                <TabItem Header="图纸管理 (Sheets)">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,10">
                            <Button Style="{StaticResource IconButton}" Tag="&#xE710;" Content="添加参数列" Click="AddParameterColumn_Click" ToolTip="添加图纸自定义参数"/>
                            <Button Style="{StaticResource IconButton}" Tag="&#xE896;" Content="导出 Excel" Click="ExportSheetExcel_Click" ToolTip="导出图纸数据"/>
                            <Button Style="{StaticResource IconButton}" Tag="&#xE8B5;" Content="导入建图/更新" Click="ImportSheetExcel_Click" ToolTip="导入Excel，存在则更新参数，不存在则自动建图纸"/>
                            <Button Style="{StaticResource IconButton}" Tag="&#xE74D;" Content="删除图纸" Click="DeleteSheets_Click" Foreground="#A03020" ToolTip="删除选中的图纸"/>
                            <Button Style="{StaticResource IconButton}" Tag="&#xE72C;" Content="刷新" Click="Refresh_Click" ToolTip="重新加载图纸"/>
                        </StackPanel>
                        
                        <DataGrid x:Name="SheetsDataGrid" Grid.Row="1" GridLinesVisibility="All" HorizontalGridLinesBrush="#E6E6E6" VerticalGridLinesBrush="#E6E6E6" RowHeight="32">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="#" Binding="{Binding Index}" Width="40" IsReadOnly="True" />
                                <DataGridTextColumn Header="图号 (Sheet Number)" Binding="{Binding SheetNumber, UpdateSourceTrigger=LostFocus}" Width="150"/>
                                <DataGridTextColumn Header="图名 (Sheet Name)" Binding="{Binding SheetName, UpdateSourceTrigger=LostFocus}" Width="250"/>
                                <DataGridTemplateColumn Header="修订 (Revisions)" Width="200">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{Binding Revisions}" VerticalAlignment="Center" Margin="5,0" TextTrimming="CharacterEllipsis"/>
                                                <Button Grid.Column="1" Content="..." Width="30" Height="22" Margin="2" Padding="0" Click="EditRevisions_Click" Tag="{Binding}"/>
                                            </Grid>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <TextBlock x:Name="SheetStatusBarText" Grid.Row="2" Text="共计 0 张图纸" Foreground="{StaticResource SecondaryText}" Margin="0,10,0,0"/>
                    </Grid>
                </TabItem>

                <!-- 图框管理 Tab -->
                <TabItem Header="图框管理 (Titleblocks)">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,10">
                            <Button Style="{StaticResource IconButton}" Tag="&#xE896;" Content="导出 Excel" Click="ExportTitleblockExcel_Click" ToolTip="导出图框参数"/>
                            <Button Style="{StaticResource IconButton}" Tag="&#xE8B5;" Content="批量导入参数" Click="ImportTitleblockExcel_Click" ToolTip="导入修改后的图框参数"/>
                            <Button Style="{StaticResource IconButton}" Tag="&#xE710;" Content="在视图中选择" Click="SelectTitleblockInView_Click" ToolTip="高亮显示选中的图框"/>
                            <Button Style="{StaticResource IconButton}" Tag="&#xE72C;" Content="刷新" Click="RefreshTitleblock_Click" ToolTip="重新加载图框数据"/>
                        </StackPanel>
                        
                        <DataGrid x:Name="TitleblocksDataGrid" Grid.Row="1">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="所属图号" Binding="{Binding SheetNumber}" Width="100" IsReadOnly="True" />
                                <DataGridTextColumn Header="所属图名" Binding="{Binding SheetName}" Width="150" IsReadOnly="True" />
                                <DataGridTextColumn Header="图元ID" Binding="{Binding InstanceIdValue}" Width="80" IsReadOnly="True" />
                                <DataGridTemplateColumn Header="图框类型 (换型)" Width="200">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <ComboBox ItemsSource="{Binding DataContext.AvailableTitleblocks, RelativeSource={RelativeSource AncestorType=Window}}"
                                                      DisplayMemberPath="Name" SelectedValuePath="Id"
                                                      SelectedValue="{Binding TypeId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      SelectionChanged="TitleblockComboBox_SelectionChanged"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <TextBlock x:Name="TitleblockStatusBarText" Grid.Row="2" Text="共计 0 个图框" Foreground="{StaticResource SecondaryText}" Margin="0,10,0,0"/>
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>
    </Border>
</Window>
